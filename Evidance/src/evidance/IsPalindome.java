/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package evidance;

/**
 *
 * @author User
 */
public class IsPalindome {

    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) {
        String[] arrs={"mim","eye","bd","wow","azad"};
        System.out.println("");
        
    }
    public static boolean IsPalin(){
        boolean status =false;
        StringBuilder=String.valueOf(str.reverse());
        if (newStr.equalIgnoreCase(s)){
            status=true;
        }
        return status;
    }
    public static int countPalindom(String[] s){
        int counter=0;
        for (int i=o;i<s.length;i++){
            if(IsPalindome(s[i])){
                counter++;
            }
        }
        return counter;
    }
}
